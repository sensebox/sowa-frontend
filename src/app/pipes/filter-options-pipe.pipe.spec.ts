import { FilterOptionsPipePipe } from './filter-options-pipe.pipe';

describe('FilterOptionsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOptionsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
