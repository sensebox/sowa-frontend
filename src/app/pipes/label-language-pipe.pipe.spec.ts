import { LabelLanguagePipePipe } from './label-language-pipe.pipe';

describe('LabelLanguagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelLanguagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
